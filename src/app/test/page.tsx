import React from "react";

const page = () => {
  return (
    <div>
      <div>
        <input type="text" placeholder="test" />
        <button>Submit</button>
      </div>
    </div>
  );
};

export default page;
